import React from 'react';
import {AppRegistry, View} from 'recat-native';

//creat a component
const App = () => {
  retuen(
    <view>
    </view>
  );
};

// Note: test renderer must be required after react-native.
AppRegistry.registerComponent('HelloWorld', () => App);
